{
  "name": "hooked-media-strategy",
  "private": true,
  "version": "1.0.0",
  "description": "Hooked Media Strategy marketing site — static HTML/CSS/JS deployed to Cloudflare Workers (static assets).",
  "scripts": {
    "dev": "wrangler dev --port 8788",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.131.0"
  }
}
